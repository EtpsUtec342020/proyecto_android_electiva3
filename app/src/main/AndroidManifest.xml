<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.electiva3.proyecto_android_electiva3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_snauto"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_snauto_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_actualizar_user_reserva" />
        <activity android:name=".activity_lista_user_reservas" />
        <activity android:name=".activity_nuevo_user_reserva" />
        <activity
            android:name=".user_detalle_orden"
            android:parentActivityName=".activity_user_ordenes" />
        <activity
            android:name=".activity_user_ordenes" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".activity_principal"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>