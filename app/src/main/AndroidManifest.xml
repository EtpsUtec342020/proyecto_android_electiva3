<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.electiva3.proyecto_android_electiva3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_lista_marca_modelo"></activity>
        <activity android:name=".flujoPlan.activity_actualizar_plan" />
        <activity android:name=".flujoPlan.activity_lista_planes" />
        <activity android:name=".flujoServicio.activity_actualizar_servicio" />
        <activity android:name=".flujoServicio.activity_lista_servicios" />
        <activity android:name=".flujoContrato.activity_lista_contratos" />
        <activity android:name=".flujoContrato.activity_new_contrato" />
        <activity android:name=".flujoContrato.activity_actualizar_contrato" /> <!-- flujo vehiculos -->
        <activity android:name=".flujoVehiculo.activity_lista_vehiculos" />
        <activity android:name=".flujoVehiculo.activity_actualizar_vehiculo" />
        <activity android:name=".flujoVehiculo.activity_new_vehiculo" /> <!-- flujo usuarios -->
        <activity android:name=".flujoUsuario.activity_lista_usuarios" />
        <activity android:name=".flujoUsuario.activity_actualizar_usuario" />
        <activity android:name=".flujoUsuario.activity_new_usuario" /> <!-- menu admin -->
        <activity android:name=".activity_admin_home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>