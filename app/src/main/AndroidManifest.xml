<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.electiva3.proyecto_android_electiva3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_actualizar_vehiculo"></activity>
        <activity android:name=".activity_new_vehiculo" />
        <activity android:name=".activity_lista_usuarios" />
        <activity android:name=".activity_admin_home" />
        <activity android:name=".activity_actualizar_usuario" />
        <activity android:name=".activity_new_usuario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>